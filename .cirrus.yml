env:
    # Cirrus Specific .Var.s (Do *NOT* Change)
    CIRRUS_SHELL: bash

task:
  name: "Build rom"
  timeout_in: 180m
  container:
      image: ghcr.io/catthehacker/ubuntu:act-18.04
      cpu: 8
      memory: 32G

  Sync_script:
      - apt update
      - apt install curl -y
      - curl -sL https://github.com/bread-up/my_tools-2/raw/6ee3065be0e0fa2e71b0250355cb6d4768ce6b88/rom/sync.sh | bash
